{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "postgres": {
      "type": "local",
      "command": [
        "npx",
        "-y",
        "@henkey/postgres-mcp-server",
        "--connection-string",
        "postgresql://sts:sts@localhost:5432/sts"
      ],
      "enabled": true
    }
  }
}
