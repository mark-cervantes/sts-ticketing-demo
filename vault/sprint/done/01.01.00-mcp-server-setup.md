---
title: "MCP Server Configuration"
priority: high
depends_on: "none"
branch: "main (no feature branch — done before sprint formally started)"
status: done
---

## What Was Built

Project-level `opencode.json` configured with MCP servers, agents granted access:

| MCP | Source | Agents granted |
|---|---|---|
| `laravel-boost` | `php artisan boost:mcp` (auto-installed) | all 4 |
| `postgres` | `@henkey/postgres-mcp-server` (npx) | backend, tech-lead, qa |
| `playwright` | global config | frontend, qa |
| `serena` | global config | all 4 |
| `context7` | global config | all 4 |

Laravel Boost installed via `composer require laravel/boost --dev` and configured via `php artisan boost:install --mcp --guidelines`. Boost auto-detected OpenCode and wrote both MCP config (to `opencode.json`) and Laravel-specific AI guidelines (to `AGENTS.md`).

## Decisions made during execution
- Used Laravel Sail scaffold (Option B from research) for dev Docker setup
- Adopted Laravel 13 instead of Laravel 11 (current default, Boost-optimized) — SPEC + ADR-001 updated
- Postgres MCP connection string set to Sail defaults: `sail:password@localhost:5432/laravel`

## Done When (all met)
- [x] `opencode.json` created with all MCP server configs
- [x] Laravel Boost installed and verified
- [x] Each agent's `tools:` declaration updated with MCP access
- [x] Committed
