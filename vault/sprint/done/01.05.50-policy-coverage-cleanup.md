---
title: "Policy coverage cleanup — CommentPolicy array syntax, forceDelete, 401 tests"
priority: low
depends_on: "01.05.00, 02.01.00 (Issue CRUD routes)"
branch: "chore/policy-coverage-cleanup"
origin: "01.05.00 tech-lead review follow-ups"
---

## What To Build

Tighten authorization test coverage. Each item is independent — knock off in any order.

### Items

1. **CommentPolicy::create array-syntax test**
   - Currently QA tests call `$user->can('comment', $issue)` (hits `IssuePolicy::comment` directly).
   - The `CommentPolicy::create(User, Issue)` signature is in place but untested via its actual invocation route.
   - When Comment CRUD lands (Sprint 02), add: `$user->can('create', [Comment::class, $issue])` assertions covering the same matrix (owner / view-share / comment-share / edit-share / unrelated).

2. **IssuePolicy::forceDelete test**
   - Coder added `forceDelete` ability as a bonus during 01.05; it's not in QA tests.
   - Add `test_owner_can_force_delete_own_issue` and `test_non_owner_cannot_force_delete`.

3. **401 unauthenticated tests**
   - Policy tests today only cover authenticated users. Once Issue CRUD routes exist (02.01), add HTTP-level tests verifying unauthenticated requests get 401 on all issue endpoints.

4. **`getShare()` memoization (OPTIONAL)**
   - When controllers check multiple abilities (view → comment → update) on one issue, `getShare()` runs N DB hits.
   - Trivial optimization: in-method static cache OR pass the share through.
   - Defer until profiling shows it matters. Not needed at Sprint 1 scale.

## Done When

- [x] CommentPolicy array-syntax tests added (item 1)
- [x] forceDelete tests added (item 2)
- [x] 401 unauthenticated coverage added when routes exist (item 3 — satisfied by `IssueCrudApiTest` after 02.01)
- [x] Memoization (item 4) — deferred; no profiling evidence justified it
- [x] Each subtask committed with a focused commit message
- [x] Merged to dev

## Changes

- `tests/Feature/Policies/CommentPolicyTest.php` — added array-syntax `CommentPolicy::create` coverage across the share matrix
- `tests/Feature/Policies/IssuePolicyTest.php` — added `forceDelete` coverage
- `tests/Feature/IssueCrudApiTest.php` — already satisfied the unauthenticated 401 endpoint coverage requirement unlocked by 02.01.00
